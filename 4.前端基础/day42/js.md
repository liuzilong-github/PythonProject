### JavaScript 简介
```
JavaScript在1995年诞生了; 
由Netscape公司,布兰登·艾奇(Brendan Eich)发明的ECMAScript客户端脚本语言;
主要应用在浏览器,在当时却不温不火.
直到后来Netscape与Sun合作，借着java的火爆程度改名叫JavaScript,开始流行起来
经过历史迭代,欧洲计算机制造商协会，European Computer Manufacturers Association 
简称:ECMA组织协会, 在2015年6月正式发布JavaScript语言的新标准 ECMAScript 6（简称ES6）
目前,简称ES6 是js的最新版本

参考网站 : https://www.w3school.com.cn/js/index.asp
```

### js中,一切皆是对象,
```
具体细分为三大对象:
    js本地对象 : 针对于js的基本语法
    jsDOM对象  : 针对于html的控制    document object model 
    jsBOM对象  : 针对于浏览器的控制   browser object model 
```

### 字符串对象
```
//获取字符串长度 length	
//清除两侧的空白 trim
//获取首次出现的位置 indexOf
//最后一次出现的位置 lastIndexOf
//连接字符串	concat	 
//截取字符串 slice	
//截取字符串 substr
//拆分字符串 split
//大小写 toUpperCase toLowerCase
//字符串替换 replace
//找索引下标 search
```
### 数组对象
```
//增加元素 push , unshift
//删除元素 delete , pop , shift , splice
//修改元素
//数组合并 concat 
//组合成字符串  join
// 数组元素反转 reverse
// 截取数组的一部分 slice
// 排序 默认升序 sort
```

### 数学对象
```
//四舍五入round
//最大值 max
//最小值 min
//绝对值 abs
//向下取整 floor 地板
//向上取整 ceil 天花板
//幂运算 pow
//开方运算 sqrt
//获取从0到1随机值   random(10,100)
//获取从 m 到 n 的随机值   5,14

```
### 日期对象
```
//获取年 getFullYear
//获取月份 getMonth 月份的索引是从 0 开始的
//获取日期 getDate
//获取小时 getHours
//获取分钟 getMinutes
//获取秒 getSeconds
```